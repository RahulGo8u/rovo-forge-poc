# Application
APP_NAME=reports-api
PORT=10000
API_PREFIX=/api/v1
APP_VERSION=0.6.0

# Required for protected /api/v1 routes. Never commit the real value.
API_SECRET_KEY=replace-with-a-long-random-secret

# Upstream Human-to-SQL service used by the SQL generator bridge.
HUMAN_TO_SQL_BASE_URL=https://reports-human-to-sql-service.onrender.com
